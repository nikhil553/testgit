#include<stdio.h>
#include<conio.h>
void main()
{
    int a[50],i,s,t,n;
    printf(enter no. of ranks);
    scanf("%d,&n");
    for(i=0;i<n;i++)
    {
        printf("enter ranks");
        scanf("%d",&d[i]);
        }
    
  for(s=0;s<n;s++)  
  for(i=0;i<n-1;i++)
  {
      if(d[i]>d[i+1])
      {
          
        t=d[i];
         d[i]=d[i+1];
          d[i+1]=t;    
          
      }   
                    
  }
  for(i=0;i<n;i++)
      printf("%d",d[i]);
    
    
  getch();  
        
}
